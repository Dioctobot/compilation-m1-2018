globals(y)
l0 : y <- copy 5;
end

globals(main)
  local x, y, y1, z, main2 :
  l0 : y <- copy 3;
  l1 : y1 <- copy 4;
  l3 : x <- copy 2;
  l4 : z <- copy y;
  l5 : main2 <- add z, x;
  l6 : main <- add main2, y;
end