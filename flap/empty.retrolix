def fact()
%rax <- copy 1;
l0 : jumpif lte %rdi, 1 -> l2, l1;
l1 : %rax <- mul %rax, %rdi;
%rdi <- sub %rdi, 1;
jump l0;
l2 : %rdi <- copy %rdi;
end

globals()
%rdi <- copy 5;
fact();
%rdi <- copy %rax;
observe_int();
end

