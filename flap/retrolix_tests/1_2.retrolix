globals (x)
l1 : x <- copy 6;
  %rdi <- copy x;
end

globals (y)
l2 : y <- copy 7;
  %rdi <- copy y;
end

globals (z)
l3 : z <- mul x, y;
  %rdi <- copy z;
end

globals (k)
  k <- sub z, x;
  %rdi <- copy k;
  observe_int (1);
end
